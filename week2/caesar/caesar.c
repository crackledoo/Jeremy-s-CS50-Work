#include <ctype.h>
#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main (int argc, string argv[])
{
    if (argc == 0)
    {

    }
    for (int i = 0, n = strlen(argv[1]); i < n; i++)
    {
        char *f = argv[i];
        if (f >= 'a' && f <= 'z')
        {

        }
    }
    if (strlen(argv[1]) != 26)
    {

    }
    printf("%s\n", argv[1]);
}